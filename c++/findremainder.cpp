#include<iostream>
using namespace std;
int main(){
    int a=14;// a means divend
    int b=3;// b means divisor
    // int r;// r means reamider
    // int q=a/b;// q means quotient
    // // a = (b*q)+r
    int r = a%b; // reaminder when a is devided by b
    cout<<r;
    

}